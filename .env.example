# Database Configuration
DATABASE_URL=postgresql://orderms:orderms_secret@postgres:5432/orderms

# PostgreSQL (for Docker)
POSTGRES_USER=orderms
POSTGRES_PASSWORD=orderms_secret
POSTGRES_DB=orderms

# JWT Configuration
JWT_SECRET=your-super-secret-key-change-this-in-production

# Server Configuration
NODE_ENV=production
PORT=4000

# CORS Origin (frontend URL)
CORS_ORIGIN=http://localhost:3000
